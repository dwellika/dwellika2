/**
 * Dwellika now uses Supabase for persistence and auth.
 *
 *   import { createClient } from "@/lib/supabase/server"  // Server Components
 *   import { createClient } from "@/lib/supabase/client"  // Client Components
 *   import { createAdminClient } from "@/lib/supabase/admin"  // Service role
 *
 * Database row types live in @/lib/types/database after running:
 *
 *   pnpm db:types
 *
 * This file is kept only as a breadcrumb for legacy imports.
 */
export {}
